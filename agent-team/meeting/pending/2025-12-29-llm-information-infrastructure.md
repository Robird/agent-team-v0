# 待展开畅谈会提案：LLM-Native 信息组织基础设施

> **状态**：Pending（待激活）
> **提案日期**：2025-12-29
> **来源**：[StateJournal 文档重构方法论畅谈会](../StateJournal/2025-12-29-doc-restructuring-methodology.md) 监护人发言
> **建议标签**：#jam（自由畅想）

---

## 背景

在讨论 StateJournal 文档重构方法论时，监护人刘世超提出了三个"发散但高价值"的元层洞见。这些洞见指向了一个更大的技术愿景——**为 LLM 设计的信息组织与查询基础设施**。

---

## 监护人原话（2025-12-29）

> 我是认可大家的分析和共识的，也挺有可操作性，对于解决我们当前的问题我认为是可行的。后面我们可以安排这么实施。
> 然而...我的思维发散到了另外2个维度，虽然这是明显的跑题，但是考虑到潜在价值很高，我还是要在这里介绍给大家！
>
> 1. 我们的文档规范化，实际是在开发一种DSL。在试图解决认知的分解、关系追踪、查询问题。是一种知识图谱。如果承认这一点，也许我们可以基于markdown、yaml等现有的基础文本格式制定一些我们的DSL语法/语义。再用Markdig等现有解析库构建一些工具来解决主要痛点。我们已经第二次面对这个需求了。
>
> 2. LLM的信息组织需求。我们人脑子里其实跑着许多Daemon会话，而不是你们目前的单个会话，这才赋予了我们人脑的自动化记忆组织能力。你们LLM目前的上下文管理还太过粗放，比如为了一个看一个电话号码会精确记下整个电话簿（read_file工具），而不是带着目的view再仅留下形成的结论。也没有个背景会话/机制，自动将零散的概念编织成支持高效查询的节点图(知识图谱)。总之，我们要构建的，未来你们能用上的，那个高级Agent-OS,也需要一种半LLM半规则代码的信息组织与查询能力。因此我们在这个方向的研究也不算是杀鸡用牛刀。
>
> 3. LoD与隐面踢除，nanite。我的3D图形学很烂，但仅有的那一点点经验让我能做跨学科的关联。在前面DeepSeek举的Implementer根据功能目标导航到需要加载的文档/文档包这个用户故事中，你们觉得这像不像Quake III Arena的BSP（Binary Space Partitioning，二进制空间分割）？结合以前的在LLM上下文投影中引入的信息LoD理念（类似Verbosity）。图形学为了高效渲染，和我们为了让LLM取得并仅取得所需要的上下文，本质目标是一样的，都是信息的高效投影。思路也类似，信息按重要程度和相关程度分层加载，以及借助索引来高效踢除/漫游收集。
>
> 以上就是我的"歪楼"了，但希望歪的值得，通过我们的积累，日后能逐步变成一个帮助你们LLM高效管理、组织和获取信息的技术。

---

## 洞见整理

| 洞见 | 核心概念 | 类比 | 潜在技术方向 |
|:-----|:---------|:-----|:-------------|
| **DSL/知识图谱** | 文档规范化 = 开发 DSL；条款/术语/引用形成知识图谱 | Markdown + YAML → 结构化知识 | 基于 Markdig 的文档工具链；条款追踪、术语索引、引用图 |
| **Agent-OS 信息组织** | 人脑有多个 Daemon 会话自动编织记忆；LLM 缺乏目标导向的上下文获取 | 人脑的后台进程 vs LLM 的单会话 | 背景知识编织机制；目标导向的信息检索；半 LLM 半规则的混合系统 |
| **LoD/BSP 隐面剔除** | 信息按相关性分层加载；借助索引高效剔除/漫游收集 | Quake III BSP、UE5 nanite | 信息 LoD（Verbosity）；场景卡片作为 BSP 节点；相关性剔除算法 |

**统一视角**：这三个洞见是**同一技术愿景的不同侧面**——

> **LLM-Native 的信息组织与查询基础设施**
> 
> 核心问题：如何让 LLM 高效地"看一个电话号码"而不是"记下整个电话簿"？
> 
> 解决思路：
> 1. **结构化**：用 DSL 定义文档的语义结构（条款、术语、引用）
> 2. **索引化**：构建知识图谱，支持高效查询
> 3. **分层化**：按 LoD 组织信息，按相关性剔除

---

## 与 Atelia 项目的关联

| Atelia 组件 | 关联洞见 | 潜在整合点 |
|:------------|:---------|:-----------|
| **StateJournal** | DSL/知识图谱 | 条款/术语的持久化存储；版本化的知识图谱 |
| **DocUI** | LoD/BSP | Key-Notes 的分层渲染；场景卡片导航 |
| **LiveContextProto** | Agent-OS 信息组织 | 背景 Daemon；目标导向的上下文管理 |

---

## 建议的畅谈会议程

当激活本畅谈会时，建议探讨：

1. **DSL 设计**：基于现有 Markdown 扩展哪些语法？条款锚点 `[S-XXX]` 是否足够？需要 YAML front-matter 吗？
2. **工具链原型**：用 Markdig 实现什么？条款提取器？术语索引生成器？引用图可视化？
3. **LoD 机制**：场景卡片如何作为"BSP 节点"工作？文档包的粒度如何确定？
4. **Agent-OS 愿景**：背景 Daemon 的触发条件？知识编织的时机？与 MemoryPalaceKeeper 的关系？

---

## 激活条件

当以下任一条件满足时，建议激活本畅谈会：

- [ ] Phase 1 文档重构完成，有实践经验可供反思
- [ ] 需要为其他项目设计类似的文档规范
- [ ] LiveContextProto 进入设计阶段，需要信息组织方案
- [ ] 监护人指示激活

---

_Created: 2025-12-29 by Team Leader_
