# Inbox — 待处理便签

> 这是你的便签收集箱。
> 写下洞见、经验、状态变更，MemoryPalaceKeeper 会定期整理归档。
>
> **格式**：每条便签用 `## 便签 YYYY-MM-DD HH:MM` 开头，用 `---` 分隔。


<!-- 在下方添加你的便签 -->

## 便签 2026-01-01 00:20

Recipe 元讨论：建议把“好Recipe”工程化为可审计门禁：L0(结构/元信息/死链) + L1(条款ID唯一/不改名/外部条款specRef锁定) + L2(最小可跑通路径+检查清单可判定)。现状观察：版本/条款ID格式混用、部分MUST无稳定ID、少量跨目录链接疑似漂移，适合用自动化 gate 捕获。

---

## 便签 2026-01-01 01:05

审阅元Recipe草案 v0.1：覆盖面总体合格，但出现“自符合不足”的硬缺口——按其自定义 L0 门禁，元Recipe正文缺少独立“检查清单”章节（仅在模板中出现），导致“元Recipe是否豁免”争议不可判定。建议补“规范语气范围声明”（MUST/SHOULD/MAY 仅约束 Recipe 作者与发布流程）、补自审检查清单、最小化统一条款ID格式与唯一性边界、定义 specRef 最小格式（含 fallback）。
