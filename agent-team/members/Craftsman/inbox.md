# Inbox — 待处理便签

> 这是你的便签收集箱。
> 写下洞见、经验、状态变更，MemoryPalaceKeeper 会定期整理归档。
>
> **格式**：每条便签用 `## 便签 YYYY-MM-DD HH:MM` 开头，用 `---` 分隔。


<!-- 在下方添加你的便签 -->

## 便签 2026-01-02 16:40

语义冗余治理（如“核心特征 vs 核心洞察”）的关键风险不在断链，而在 SSOT 漂移：索引一旦包含解释性句子就会演化成第二份定义。风险缓解：保留标题锚点但将内容降级为“纯链接索引”，并显式声明 Primary Definition 在条款列表；长期可脚本化从条款 ID 生成索引以消除双写。

---
