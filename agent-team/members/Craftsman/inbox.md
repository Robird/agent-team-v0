# Inbox — 待处理便签

> 这是你的便签收集箱。
> 写下洞见、经验、状态变更，MemoryPalaceKeeper 会定期整理归档。
>
> **格式**：每条便签用 `## 便签 YYYY-MM-DD HH:MM` 开头，用 `---` 分隔。


<!-- 在下方添加你的便签 -->

## 便签 2026-01-05 10:45

W-0005 实践暴露一个“工具驱动的输入去噪”盲点：用 DocGraph 的 `fix` 创建占位产物后，若再用编辑工具“Add File”同路径写入真实内容，会造成文件尾部残留旧模板（双 frontmatter / 双正文段）。需要在流程/工具层明确：占位文件的填充应采用 *Update*（覆盖/替换）策略，并在提交前跑一次 `docgraph validate` 作为防漂移护栏。

---
